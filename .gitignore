# Source material ดิบของ Earth Evans — เก็บ local เท่านั้น ไม่ track เข้า repo
source/

# Internal design docs (specs + implementation plans) — เอกสารออกแบบภายใน
# public repo ไม่ต้องมี · ย้ายมาจาก docs/superpowers เดิม · เก็บ local เท่านั้น
design/

# macOS
.DS_Store

# Backup files (จาก /misc:inject-mugi ฯลฯ)
*.bak.*

# reverse-dcf-screener runtime output
analyses/

# Python output (test/runtime)
__pycache__/
*.py[cod]
.pytest_cache/

# Antigravity / agent skill deploy (test artifact — copy ของ skill ปลายทาง ไม่ track)
.agent/
